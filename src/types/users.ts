export type UserId = number;
export type TeacherDisciplineId = number;
export type UserName = string;
export type Password = string;

export enum RoleName {
    STUDENT = "student",
    TEACHER = "teacher",
    ADMIN = "admin",
}
